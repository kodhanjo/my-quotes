import { PlatesDirective } from './plates.directive';

describe('PlatesDirective', () => {
  it('should create an instance', () => {
    const directive = new PlatesDirective();
    expect(directive).toBeTruthy();
  });
});
